package com.example.demo.constant.auth;

/**
 * Hằng số liên quan header Authorization / Bearer (tránh hard-code chuỗi rải rác).
 */
public final class HttpAuthConstants {

    public static final String BEARER_PREFIX = "Bearer ";

    private HttpAuthConstants() {
    }
}
